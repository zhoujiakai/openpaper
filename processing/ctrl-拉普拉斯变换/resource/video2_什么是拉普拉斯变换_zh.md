# 到底什么是拉普拉斯变换？

_中文字幕（Google 翻译）_

---

[在 criblate.com 提交字幕更正]
您所看到的，是您和我将在本视频中构建的有点复杂的图表，它是一个可视化，揭示了用于研究微分方程的最强大工具之一背后的含义。它被称为拉普拉斯变换。这是其中一种工具，作为一名学生，您可以学习如何使用它来求解方程，但对它实际在做什么却完全一无所知。想想学习如何驾驶汽车与学习内燃机如何工作。

两者都是值得追求的，一个不一定比另一个更好，而且事实上驾驶可能更实用。但打开引擎盖并了解内部机制是一件令人深感满足的事情。同样，我们这段视频的主要目标是打开引擎盖，向您展示这个物体内部等待我们的一些美丽的数学。严格来说，如果您的唯一目标是解方程，那么我想展示的很多内容都是不必要的。

也就是说，对于你们当中学微分方程的学生来说，我认为这里的内容应该会让你们要记住的东西更容易记住，在解剖这台机器，一点一点研究它之后，你和我将把一切都试驾一下，看看解一个具体的、非常有趣的微分方程是什么样子的。现在，在我在屏幕上给出定义之前，让我们先讨论一下拉普拉斯变换试图解决什么问题。我们在前一章中对此进行了很多介绍，这里有两个主要想法值得重申。第一，你需要理解指数函数，我总是将它们写成 e^(st) t 代表时间，然后 s 是一个数字。

它决定了我们所讨论的具体指数，但对于本主题来说非常重要的是，我们将给予 s 接受复数值的自由。我们在前一章中更全面地介绍了这一点，但简单的总结是，如果 s 有虚部，则函数的输出随着时间的流逝而在复平面中旋转。当 s 的实部为负时，幅度会随着时间的推移向零衰减，但如果它恰好为正，则意味着幅度会增长，即呈指数增长。如果其中任何一个感觉不稳定，或者如果您想了解为什么这是真的，请观看前一章，但从现在开始，我假设每个人都对这个概念感到满意。

但我们为什么要关心呢？

为什么这些功能引起热议？嗯，这是因为你需要知道的第二件事，那就是很多函数，尤其是物理学中出现的函数，可以表示为指数块的组合。有一个非常友好的例子，t 的余弦，对于在本课中反复回顾会有所帮助。这个波动函数可以分解为两个纯虚数指数之和。

基本上，如果你采用 e^(it) ，那么你会逆时针旋转，而 e^(-it) 会以另一种方式旋转，所以当你将这两个加在一起时，也许想象一下将两个旋转向量从头到尾相加，虚部相互抵消，剩下的就是输出保持锁定在实数线上，来回振荡的东西。现在，这个和在负二和正二之间，但余弦只在负一和一之间，所以最后你把所有的东西都乘以二分之一。更复杂的函数可能会分解成更多指数部分。例如，在本课程的后面部分，你和我将深入研究一种称为驱动谐振子的东西，它基本上是弹簧上的一个质量，受到某种外力的影响，在我们的例子中，它会随着时间的推移而振荡。

作为剧透，描述该方程的解最终看起来像是四个指数部分的总和。其中两个部件以与弹簧的自然共振频率相匹配的方式振荡和衰减，然后另外两个部件以与外力相匹配的方式振荡。因此，对于像这样的情况和许多其他情况，我们想要的是某种工具，某种数学机器，您可以在其中输入一个函数，甚至是描述该函数的微分方程，并且该机器将以某种方式向我们揭示该函数分解为哪些特定的指数部分。也就是说，它揭示了指数中的这些值是什么，以及相应的系数是什么。

现在您可能想知道为什么是指数级的，为什么不关注其他事情。简而言之，对于这些函数，当你求导数时，它看起来与乘以某个数字（即 s）完全相同。最后你会看到，这意味着同一台机器可以让我们将函数剖析成指数部分，也可以让我们将微分方程转化为代数，本质上是因为无论你在哪里看到导数，它都会变成乘以 s。现在，在这样的背景下，我们如此关注复值指数，工程师为我在左侧描述的这个复平面有一个特殊的名称，代表指数中该项 s 的所有可能值。

他们称之为 s 平面。

一个有用的心理图像是将 s 平面上的每个单独点视为对整个指数函数 e^(st) 进行编码。对于这张图片，我显示的图表仅描述了输出的实数部分，但请记住，每个点都代表完整的复值函数。不过，这些图表足以满足直觉。您会注意到更大的虚部对应于更快的振荡，然后当您的眼睛从左到右扫描时，实部反映了衰减或增长。

因此，以此为我们的目标，你和我今天要构建的机器，揭示函数如何分解为指数部分的机器，正如你无疑猜到的那样，是拉普拉斯变换。现在这个词“变换”有点有趣，基本上就像函数是接受一个数字并吐出一个新数字的东西一样，我们经常在数学中使用“变换”这个词来表示更元的操作，即接受整个函数并吐出一个新函数。对于拉普拉斯变换，典型的约定是使用用于命名原始函数的大写版本来命名我们的新函数。在这种设置中，我们的原始函数将时间作为输入，新的转换函数具有新的不同类型的输入，即复数值 s。

我认为在我们提取定义并开始剖析它之前快速预览这个新函数的实际功能是有帮助的。假设您的原始函数 f(t) 实际上可以分解为几个指数部分的总和。当你应用这个拉普拉斯变换时，给你这个新变量 s 的新函数，如果你要在 s 平面上绘制这个新函数，以我将在一分钟内解释的方式，你会看到这些尖峰位于 s 的每个值上方，对应于这些指数部分之一。这些尖峰有一个奇特的名字，它们被称为函数的极点。

因此，即使您事先不知道如何将函数分解为指数之和，如果您了解这个转换版本，特别是如果您了解极点，那么就可以为您揭示这些指数部分是什么。此时，您可能很想知道这个东西实际上是如何定义的，而在它的全部荣耀中，这就是该定义的样子，我们可以将其视为两个单独的步骤。首先，将函数乘以表达式 e^(-st)，然后，将结果随着时间从 t 等于 0 到无穷大进行积分。我们将在一分钟内讨论该积分及其细微差别，但目前将重点放在内部表达上。

s 项是新引入的参数，是新变换函数的输入。

正如我所说，这是一个复数。每次您在视频中看到字母 s 时，它都是一个复数。我喜欢思考这个问题的方式是，你可能会想象在 s 平面上围绕这个值 s 自由移动，这有点像嗅探以找到哪些特定的指数函数与我们的函数 f(t) 密切相关。例如，假设我们的函数是 t 的余弦，正如我们一分钟前讨论的那样，我们已经知道它可以分解为两个指数的和，e^(it) 和 e^(-it) 我已经预览了这样的想法，当您绘制最终结果时，您应该看到 s 的关键值上的这些尖峰，在这个示例中这意味着极点高于正 i 和负 i，并且您已经可以通过此示例并替换获得一点直觉t 余弦的扩展表达式。

看着这个扩展，我希望你花点时间思考一下这两个术语的乘积。当你将两个指数相乘时，这些指数的内容相加，所以剩下的就是 e 的 i 减 s 乘以 t 的结果。让我继续将该值绘制在右下角，并将 s 平面保留在右上角。就像我们已经看到的指数一样，随着时间从零到无穷大，它会振荡和衰减，或者可能会增长。

具体的形状取决于我们如何设置该值 s。现在，在这种情况下，当您围绕该值移动时，有一个特定值会导致独特的无聊行为。如果将 s 设置为等于 i，则指数中的该项将变为零，因此整个函数看起来就像 e^0，它停留在常数 1。这是几乎所有 s 值的关键思想。

像这样的指数会随着时间而变化，通常看起来像某种螺旋，但在我们正在寻找的特殊值下，在这种情况下，s等于i，相反，事情会陷入一个常数。所以，退一步来说，如果你是一名数学家，并且你正在尝试发明某种机器来检测潜伏在函数内部的指数部分，例如，检测余弦有一个 e^(it) 和一个 e^(-it) 潜伏在其中，你可能会问自己，我能做些什么来检测像这样的和中的一项秘密地只是一个常数吗？本质上，这就是积分所扮演的角色，它将所有内容包裹在完整的定义中，随着时间从零到无穷大进行积分。一方面，如果您熟悉微积分，您也许能够预测为什么这会导致 s 的所需值出现某种急剧上升。

如果将一个常数从零一直积分到无穷大，它就会爆炸。

但实际上这里有一些细微差别。首先，积分内的函数采用复数值。对于我们这些好奇以令人满意的方式解释事物的人来说，这自然提出了一个问题。您如何看待集成一个复杂的值函数？

如果您愿意的话，我想在接下来的 10 分钟左右的时间内真正深入研究并剖析这样的积分的真正含义，如何将其可视化，并解释为什么我所显示的图表示与该积分的字面含义略有不同的内容。与任何新的数学问题一样，最好从简单开始，然后逐渐复杂。因此，首先，让我们忽略这个函数 f(t) 本身，只关注对 e^(-st) 部分的积分。为了热身，在深入了解这一切的复杂性之前，我们假设 s 是一个实数，这意味着这是一个友好的实值函数，我们可以像平常一样用图形来绘制它。

通常，当您第一次学习微积分时，您会学习将积分解释为告诉您这样的图形下的面积。例如，如果您将值 s 设置为 1，并且执行实际计算此积分的过程，您知道，您采用反导数，您在两个不同的边界处取差值，因为存在无穷大，您可以说您在该边界处取极限值，所有结果的方式是该表达式等于 1。您可以将其解释为告诉您该图下的面积为 1，这很有趣。我想这意味着这个无限尾部下方的所有区域都足以填充这个单位正方形的其余部分。

然后，如果我们重新引入该值 s 并将其设置为非 1 的值，效果就是在水平方向上挤压图形。如果将函数的输入乘以某个常数，效果总是如此。因此，该图下方的面积一开始为 1，现在必须为 1 除以 s。我希望你记住的关键一点是，如果我们让 s 变得越来越小并接近 0 的值，那么该区域会变得越来越大，实际上接近无穷大，并且也很快接近无穷大。

但图表并不是可视化函数的唯一方法，面积也不是理解积分的唯一方法，你应该养成多动动脑子的习惯。

让我向您展示另一种思考这个问题的方法，一旦我们让该函数采用复数值，这种方法就会更容易推广。考虑这个介于 0 和 1 之间的积分，即具有单位长度的积分。我希望你把图表下面的所有区域想象成一个水池，你让它晃动直到水变平。这个池子的高度告诉你这个函数在 0 到 1 之间的平均值。

然后因为这个池子的宽度只是 1，所以它的面积和它的高度是一样的。因此，当超过一个单位间隔时，顶部的积分会告诉您该间隔上函数的平均值。类似地，从 1 到 2 的积分将告诉您该函数在从 1 到 2 的区间内的平均值。然后，与您继续沿一堆其他单位区间积分时的情况相同。

那么，如果你想要从 0 到无穷大的积分，你可以考虑在这些区间上取所有这些平均值并将它们加在一起。这是我们可以合作的。现在让我们看一下复杂的情况。S 将是某个复数，然后当时间从 0 到无穷大时，函数 e^(-st) 围绕复平面循环和衰减。

它循环、衰减或增长的具体方式取决于 s 的值，我们将为每一个通过复平面获得一条不同的路径。现在，如果您想在单位间隔上积分此函数，假设从值 t 等于 0 到 t 等于 1，想象一下对该范围内的所有输出进行采样，然后找到平均值，即所有这些点的质心。这个平均值就是这个积分的意义，我会用一个小箭头来表示。实际上，如果我们将这个积分放在右下角自己的复平面中，将会很有帮助，因为我们即将开始将它们全部相加。

如果你让 t 的范围从 1 到 2，你做同样的事情，取该区间的平均值，用箭头表示，然后将该箭头添加到我们右下角的内容中，得到的总和基本上告诉你从 0 一直到 2 的积分。

然后我们重复。你取 2 到 3 之间的平均值，然后相加，再取 3 到 4 之间的平均值，相加，然后不断地继续下去。您看到的这个螺旋式求和的极限点是 e^(-st) 从 0 到无穷大的积分值，这正是我们想要理解的表达式。当我们围绕输入 s 移动时，这个螺旋式求和的结果值可能会在复平面上徘徊。

作为一个快速的健全性检查，让我将 s 的值移至输入 1。这样就不会出现振荡，因为没有虚部，并且您会注意到所有这些小箭头叠加起来最终出现在数字 1 上。这应该是有意义的。当我们以更熟悉的方式将积分解释为曲线下的面积时，我们看到当 s 等于 1 时，该值计算为 1。

就像以前一样，如果我让 s 接近 0，变得越来越小，那么得到的积分就会越来越大，迅速接近无穷大。在这个新图中，我们可以看到，如果 s 以不同的方向远离 0（垂直移动），所得积分也会变小，但原因却大不相同。函数中的所有振荡使我们在该向量和中得到更多的抵消，因此输出变得更接近 0。接下来我要做的是绘制这个值。

所以看看我们螺旋式上升的总和收敛到的那个点。我希望你将其视为复平面中的一个小向量，具有大小和方向的东西。为了获得一点奇特的效果，让我们取这个幅度，并将其绘制在 s 平面中的值 s 之上。因此，当我更改该值 s 时，它会更改生成的积分，我们的输出幅度可能会增大或缩小，并且当它这样做时，我们将在 s 平面上绘制结果。

我将暂时让其处于自动驾驶状态，其中 s 将在飞机上徘徊。

当它这样做时，花点时间思考为什么我们会得到我们所看到的形状。基本上，s 的虚部越大，表达式中的螺旋越多，意味着抵消更多，因此输出的幅度越小。如果我绘制 s 的许多可能值的完整图，结果如下所示。最明显的特征是，如果 s 越来越接近 0，那么输出的幅度就会越来越大，这是有道理的。

实部越小意味着衰减越慢，虚部越小意味着抵消越少。现在，就目前情况而言，我只是绘制了该输出的大小，但当然它包含的信息比这更多，它也有一个方向，所以如果我们将该输出的每个可能的方向与一种独特的颜色相关联，那么我可以做的一件事就是为图表着色，让我们对输出的外观有更丰富的了解。关于此图，您可能已经注意到的另一件事是，它显然不是在实部为负的 s 值上绘制的。并思考这些价值观的实际含义。

当 s 的实部为负数时，函数 e^(-st) 呈指数增长，并且积分的螺旋和会爆炸，它不会收敛。所以这些值还没有定义，至少目前还没有定义。我们很快就会谈到一个奇特的想法。在边界上，事情变得有点有趣。

如果 s 是纯虚数，则值 e^(-st) 只是绕一圈，纯旋转，既不增长也不衰减。当我们玩这个沿着不同间隔取平均值并将它们加在一起的游戏时，我们在右下角得到的向量和只是不断地螺旋式旋转，令人作呕。现在一方面，这还没有收敛，没有接近的特定值。然而，理解它的意义并不难。

如果你让 s 的值得到哪怕只是一点点实数分量，那么我们的函数就会衰减，并且我们的螺旋式求和确实会接近一个明确的具体值。

然后，如果你慢慢地去掉 s 的实部，让它接近虚数轴，那么右下角的积分结果就会明确地接近一个明确的值。你也可以在情节上看到，它显然想要承担一些价值。事实上，我可以准确地告诉你它想要收敛到什么值。回想一下实值情况，我们看到积分等于 1 除以 s。

这是一个纯粹的分析事实，即使 s 是复数也是如此。也许这就是您所期望的，但是在更丰富的复数情况下这应该保持正确，这一点并不明显。为了直观地检查至少一个例子，我们只是非常仔细地观察当 s 接近虚数常数 i 时会发生什么。如果您关注右下角发生的情况，您会发现积分正在接近负 i。

事实上，1 除以 i 的结果是负 i。所以我们有这个漂亮而简单的方程来描述我们的积分，但有趣的是，这个右手边，1 除以 s，在平面上的任何地方都有定义。我可以绘制结果，这就是它的样子。现在，您可能会对 s 等于 0 感到惊讶，但几乎在任何地方，它都有一个明确的值。

现在需要明确的是，积分本身显然不会收敛于平面的左半部分，因此严格来说，在该区域中方程没有任何意义。举个例子，考虑将 s 设置为负 1。在右侧，1 除以负 1 得到负 1，但我认为您会同意将 e^t 从 0 积分到无穷大看起来肯定不像负 1。然而，这给我们带来了复值函数的一个令人着迷的方面，它与实值函数的世界完全不同。

这就是所谓的分析延拓。

从某种意义上说，这些超出收敛范围的无意义值仍然可以反映其真正收敛的表达式的有用含义。尽管接下来的内容超出了您驾驶汽车所需了解的范围，但它是一个美丽的数学部分，也是解释我为您绘制的情节的最后一块拼图。这是我的想法。假设您有某个函数，并且它仅在有限的域上定义。

如果这是一个真正的有价值的函数，并且您想扩展定义以包含更大的域，那么您基本上有无限多种选择来执行此操作。即使你添加一些约束，假设你的函数是平滑的，因为它到处都有导数，并且你希望你的扩展也平滑，那么你仍然有无限的选择。它有点像松软的意大利面条。然而，复值函数受到的限制要大得多。

如果你只在有限的域上定义一个函数，就像我们的积分只在半个平面上收敛，并且如果该函数在具有明确定义的导数的意义上是好的，然后如果你想以一种保持它良好的方式扩展该函数，再次在具有导数的意义上，那么有一个很好的小定理告诉我们会发生以下两种情况之一。要么没有办法扩展它，要么即使有办法，那也是唯一的。这非常令人惊讶。您可能认为自己有无限的选择，但事实并非如此。

当这个扩展确实存在时，它有一个奇特的名字。我们称之为原函数的解析延拓。数学中一个非常强大的主题是，有时您可以通过理解函数的完整扩展版本，特别是理解该完整扩展版本中的极点，来发现有关函数的隐藏信息。在我们研究拉普拉斯变换的背景下，相关性在于，当 s 的实部足够大时，定义该变换的实际积分通常仅在半个平面上收敛。

然而，绘制和理解暴露函数所有极点的完整扩展版本非常有帮助。

正如我之前提到的，极点暴露了我们正在寻找的指数碎片。回顾一下我们一直关注的这个热身示例，正如我所说，e^(-st) 的积分只有在 s 的实部为正时才会收敛。在那个半平面上，它等于 1 除以 s。1 除以 s 到处都有定义，并且从具有导数的意义上来说这很好。

所以我们说这是积分的解析延续。这个函数是极点的最纯粹的例子。你说它有一个高于 s=0 的极点，我的意思是，它看起来近似于在该点周围除以 0。不难看出这个名字的由来。

该情节看起来有点像马戏团的帐篷，该点上方有一根杆子。精彩的。这实际上是一个非常有用的结果。我们现在花了很多时间研究的这个积分实际上是最简单的可能函数之一的拉普拉斯变换，即 1 处的常数函数。

该常数函数转换为 1 除以 s，您应该在脑海中将其视为 s=0 之上的极点。这看起来像是一个简单的例子，但几乎免费，我们可以挤出一个更一般的结果，即任何指数函数的变换也看起来像一个简单的极点。它只会高于 s 平面上的其他值。在这里，让我们花点时间来实际思考一下。

如果我要求你输入一个像 e^(1.5 * t) 这样的函数，我不知道，会发生什么？

好吧，然后将积分内的表达式组合起来，就得到 e^{(1.5 - s)t}，这与我们刚刚看到的所有内容几乎相同，只是移动了 1.5。如果你愿意，我可以拉出相同的视觉效果，它剖析和解释了积分，如右下所示，同样，积分内部的函数如右上角所示，我们正在玩相同的求​​平均值的游戏。但这张图中没有任何实质性改变，本质上与我们刚才看到的相同，唯一的区别是，现在，发生爆炸并且我们看到极点的特殊值是 s = 1.5，而不是 s = 0。象征性地，一点点重新排列表明这个关键积分几乎与我们刚刚研究的积分相同，唯一的区别是 s 已被 s - 1.5 取代。

因此，我们可以用圆圈写下的新结果是，指数函数的变换看起来像 1 除以 s - 1.5。当然，1.5 没有什么特别的，我可以用任何常数 a 替换它。如果您从这段视频中只记得一个事实，那就是这个。指数函数 e^(at) 的拉普拉斯变换是 s 的一个新函数，在 s = a 上有一个简单极点。

这是我之前提到的关键思想，变换函数中的极点暴露了原始函数的指数部分。充实这个想法的最后一步是让我们自己相信这对于组合是有效的。举个例子，让我们把 t 的余弦值拉回到我们的好朋友身上。在这里，我们有两种研究方法的选择，我们可以象征性地思考它，然后，为了好玩，让我们将它插入同一个视觉机器中，看看它是什么样子。

好吧，象征性地，正如我们之前讨论的，余弦可以分解为 0.5 e^(it) + 0.5 e^(-it)。接下来您可以做的是将这个外部表达式分成两个不同的部分。一个看起来像是 e^(it) 变换的一半，另一个看起来像是 e^(-it) 变换的一半。用行话来说，拉普拉斯变换是线性的，这意味着如果内部有一些东西的缩放和，您可以将所有内容分解为这些内部部分的变换的相同缩放和。

在我们的例子中，因为我们刚刚了解了如何进行简单指数函数的变换，所以这里的整个表达式可以折叠为看起来像 1 除以 s - i。

一个在 i 处有一个简单极点的东西。然后整个表达式可以折叠成 s + i 上的 1，即极点位于负 i 处。因此，当您阅读整个表达式（两个分数的总和）时，您脑海中应该浮现的图像是一个在 i 上方和负 i 处有两个不同尖峰的图。事实上，如果我们有一点乐趣，尝试将 t 的余弦表达式 e^(-st) 插入到我们构建的大型复杂积分机中，您确实会看到一个极点高于 i 且极点 i 为负的图。

这次，图表明显更加复杂，如果我对你诚实的话，符号推理可能是理解最终答案的更简单的方法。但你和我来这里是为了找点乐子，不是吗？深入研究我们正在使用的机器的每个活塞和阀门。因此，让我们看看是否可以花一两分钟来尝试理解我们正在查看的内容。

在右上角，我再次展示了积分内部的函数，即 t 乘以 e^(-st) 的余弦。但现在它更像是混乱的曲线。为了建立一点直觉，让我将 s 设置为一个小的、纯虚数，比如 0.2 i。在右上角的图中，我将继续添加一个仅显示 e^(st) 部分的向量，对于 s 的小虚值，该向量旋转得非常慢。

在完整的函数中，该项乘以余弦，因此它追踪的路径会来回振荡，给我们这个漂亮的花瓣图案。和以前一样，我们可视化积分的方式是沿着不同的单位间隔取平均值，然后将它们加在一起，就像一个大的从头到尾的向量和。在这种情况下，添加这些向量的图实际上非常好。看起来就像一个小星星图案一圈又一圈。

严格来说，这并不收敛。

左边绘制的是分析延续。如果你想让它收敛，你甚至可以在该值 s 上添加一点点实数分量，这意味着随着时间的推移，函数会稍微衰减一点，甚至一点点衰减也足以使右下角的总和收敛到一个清晰、明确的值。现在请注意，当我增加虚部时会发生什么，并且指数的频率变得更接近余弦的频率。你得到的是越来越多的对齐，从而产生更大的总积分。

事实上，当虚部为 1 时，意味着指数的振荡与余弦的振荡完全一致，那么它所描绘的路径仍然完全局限于平面的右侧，结果是积分类型被抛弃到右侧。从那里开始，如果我减少 s 的实部，意味着衰减越来越少，那么积分就会越来越接近无穷大，这就是为什么我们会看到一个高于该值的极点。顺便说一句，退后一步，对于任何碰巧观看我多年前制作的有关傅里叶变换的视频的人来说，如果所有这些看起来非常熟悉，那是因为它们基本上是同一件事。当 s 是纯虚数时，拉普拉斯变换几乎与傅里叶变换相同。

这不是完全相同的表达式，积分的下界为零，而不是负无穷大，并且该指数中的常数有不同的约定，但本质实际上是相同的。现在，傅里叶变换和拉普拉斯变换之间的这种关系将在下一章的故事中发挥更大的作用，但在这里我想快速强调一下，从某种意义上说，拉普拉斯变换是一种概括。它的作用是探测函数的排列情况，不仅与纯虚数指数，而且与任何指数。现在，回顾一下 t 余弦的拉普拉斯变换的符号结果，如果你去查找它，比如说在拉普拉斯变换的大表中，这实际上不是它看起来的样子。

首先，考虑内部具有任意角频率 omega 的更一般的余弦波是常见且有用的。这里唯一的变化是，无论您在哪里看到 i，都将其替换为 omega 乘以 i。因此，您可以将最终的表达式理解为告诉您 omega i 处有极点，而 omega i 处为负。但即便如此，这也不是您在表格中看到的内容。

让我继续在这里自动运行一些代数，它将结合这两个术语。

当一切尘埃落定后，你最终得到的是 s 除以 s 的平方加上欧米伽的平方。现在这就是您实际看到的表达式。事实上，我提出这个问题的原因是我想谈谈这个例子的整个逻辑如何反过来流动。想象一下，您事先并不知道余弦可以分解为两个指数之和，但您非常精通分部积分。

我不会在这里详细介绍细节，但您可以在这里直接计算这个等式，本质上是直接计算变换的定义。从那里，您可以使用一种被奇特地称为部分分数分解的过程，将该分数分解为两部分，清楚地暴露 omega i 和负 omega i 处的极点，并且还暴露一半的系数。这足以告诉你潜伏在里面的指数碎片是什么。这种逻辑流程实际上更能反映在实践中使用这种转换的感觉。

在这一点上，我们的下一步是试驾这台机器，看看求解实际微分方程是什么样子。现在，我最初的计划是用一个可行的例子来结束这个视频，但考虑到时间，考虑到这都是系列的一部分，给你一个站起来、伸展身体、反思一切的机会可能会更好一点，让我们把它放在后续章节中。该视频的关键要点是，当一个函数可以分解为指数部分时，拉普拉斯变换如何将这些部分揭示为 s 平面上方的极点。但我想让你知道我们还没有完全理解它的普遍性。

大多数函数不能像这样表示为离散指数和。尽管如此，该变换提供了一种非常强大的方法来将更多函数表示为指数组合。只是你在一个连续的范围内组合，而不是一个离散的范围。回到驾驶汽车与学习发动机如何工作的类比，还有第三个更深层次的理解，那就是知道如何为自己制造汽车。

在本序列的最后一章中，我想向您展示如何从头开始重新发明拉普拉斯变换，它与傅里叶变换和傅里叶反演有何关系，以及如何将其视为超出这些离散指数和的更广泛的函数族。

我们在那里见。

