# 困惑点的问答记录

### Q1: Geoffrey Hinton 的求学经历和研究方向是什么？

> 时间：20260601周一
>
> 位置：7 — 1.6 Geoffrey Hinton访谈

**求学经历：**

- **1966年，中学**：受同学 Inman Harvey 启发，对大脑全息图产生兴趣。Harvey 读到 *Scientific American* 上一篇关于全息图的文章，发现全息图的"优雅降级"特性（碎片也能重现完整图像）可用来解释大脑记忆，分享给了辛顿。两人在1966年暑假花三个月横穿美国和墨西哥，一路讨论大脑工作原理。
- **1966-1970年，剑桥大学**：攻读实验心理学学士，同时旁听哲学。
- **1972-1978年，爱丁堡大学**：AI博士学位。英国体系下没有单独的硕士学位，本科毕业直接读博。
- **1978-1982年**：加州大学圣地亚哥分校，博士后。
- **1982-1987年**：卡内基梅隆大学，任教。1987年因不满DARPA用军事经费资助AI研究，离开美国搬到加拿大多伦多大学。

**研究方向：**

- **博士期间**：研究用神经网络做视觉理解，当时AI主流是符号主义，方向不被认可。
- **1985年**：提出玻尔兹曼机。**1986年**：提出反向传播算法。
- **2006年**：提出深度信念网络。**2012年**：AlexNet引爆深度学习，同年提出 Dropout。
- **2017年**：提出胶囊网络。
- **2018年**：获图灵奖（与Bengio、LeCun共同获奖）。
- **2023年**：离开谷歌，此后专注于AI安全与风险警示。2024年获诺贝尔物理学奖（与John Hopfield共同获奖）。现任多伦多大学名誉教授。

**辛顿给“深度学习“学习者的建议：**

1. 文献读一点即可，不要读太多。读到直觉告诉你"大家都在犯错"时就停下来，自己去做好它。
2. 相信自己的直觉。别人都说是垃圾的想法，往往是真正的好想法。
3. 永不停止编程。动手复现论文，过程中才会发现让算法真正生效的"小技巧"。
4. 找信念相似的导师。导师对研究方向有切身信念时，才能给出最有价值的指导。

---

### Q2: 辛顿的胶囊网络（Capsule Network）研究历史与现状？

> 时间：20260601周一
>
> 位置：7 — 1.6 Geoffrey Hinton访谈

**研究历史：**

- **1979年**：辛顿已有胶囊网络的直觉想法。
- **2011年**：首次以"Transforming Auto-encoders"形式正式提出。
- **2017年**：《Dynamic Routing Between Capsules》发表于NIPS，引起广泛关注。
- **2018年**：提出"Matrix Capsules with EM Routing"改进版本。
- 此后学术界仍有少量论文（2024-2025年也有），主要用于小样本、可解释性等场景，但在大规模数据和算力下始终未能匹敌Transformer。辛顿本人2023年离开谷歌后已转向AI安全方向，不再主力推进胶囊网络。

---

### Q3: 深度信念网络（DBN）和我们常说的深度神经网络有什么区别？

> 时间：20260601周一
>
> 位置：7 — 1.6 Geoffrey Hinton访谈

结构上都是把多层堆起来，本质就是深度神经网络。区别不在结构，在**怎么训练**：

**DBN（2006年）：逐层预训练。** 每层受限玻尔兹曼机（RBM）先以无监督方式单独训练，学出有意义的表示，逐层往上搭。全部搭完后，再用标签做反向传播微调。

**现代DNN：端到端训练。** 初始化权重（He/Xavier），直接跑反向传播，一口气训到底。

**为什么当时需要DBN？** 2006年没有ReLU（用sigmoid/tanh），没有BatchNorm，没有ResNet，没有好的初始化方法。深层网络梯度直接消失，根本训不动。辛顿用逐层预训练绕过这个问题——相当于让每层各自"热身"找到好的起点，再统一微调。

**为什么DBN后来退出了？** ReLU解决了梯度消失，BatchNorm稳定了训练，Xavier/He初始化让起步就不差，端到端直接训练变得可行。逐层预训练退出历史舞台。

**今天的回响：** 这个思路后来换了形式复活——BERT、GPT本质也是"先无监督预训练，再微调"，把RBM换成了Transformer。

---

### Q4: 逻辑回归的成本函数是怎么推导出来的？似然函数是什么？

> 时间：20260601周一
>
> 位置：25 — 2.18 逻辑回归损失函数的解释

**推导过程：**

给定 x 时 y=1 和 y=0 的概率：

$$
p(y=1|x) = \hat{y},\quad p(y=0|x) = 1 - \hat{y}
$$

合并为一个公式（伯努利分布）：

$$
p(y|x) = \hat{y}^y (1-\hat{y})^{(1-y)}
$$

独立同分布假设下，所有样本的似然函数（连乘）：

$$
L = \prod_{i=1}^{m} p(y^{(i)}|x^{(i)}) = \prod_{i=1}^{m} \hat{y}^{(i)y^{(i)}} (1-\hat{y}^{(i)})^{(1-y^{(i)})}
$$

取对数得到对数似然（连乘变连加）：

$$
\log L = \sum_{i=1}^{m} [y^{(i)} \log \hat{y}^{(i)} + (1-y^{(i)}) \log(1-\hat{y}^{(i)})]
$$

最大化对数似然等价于最小化负对数似然，除以 m 即得成本函数：

$$
J(w,b) = -\frac{1}{m} \sum_{i=1}^{m} [y^{(i)} \log \hat{y}^{(i)} + (1-y^{(i)}) \log(1-\hat{y}^{(i)})]
$$

**似然函数是什么？**

似然函数衡量的是：**给定模型参数，观测到当前这批数据的概率有多大**。

- 单个样本：p(y|x) 表示给定参数 w,b 时，模型对样本 x 预测正确标签 y 的概率。
- 所有样本：假设样本独立同分布，观察到全部 m 个样本的概率就是连乘——这就是似然函数 L(w,b)。
- 目标：找到参数 w,b，使得"观察到这批数据"这件事发生的概率最大（最大似然估计）。

似然函数以参数为变量、数据为已知条件。同样一批数据，不同参数对应不同的"可能性"，选最可能的那个。

连乘不好优化，取对数变连加（单调变换不影响最大值点），最后加负号、除以 m，就得到了最小化形式的成本函数。

---

### Q5: Pieter Abbeel 是谁？

> 时间：20260602周二
>
> 位置：26 — 2.19 Pieter Abbeel访谈

加州大学伯克利分校教授，强化学习和机器人领域的知名研究者。

- **博士**：师从吴恩达，斯坦福大学，2008年毕业。论文方向是学徒学习（apprenticeship learning）——让机器人通过观察人类演示来学习。
- **Berkeley Robot Learning Lab**：创始人，研究方向聚焦深度强化学习、机器人操控、示教学习。
- **Covariant**：2017年联合创立，将AI机器人用于仓储物流分拣。
- **Gradescope**：联合创立的教育科技公司，后被Turnitin收购。
- **学术贡献**：Trust Region Policy Optimization（TRPO）共同提出者之一，在将深度强化学习落地到真实机器人方面做了大量开创性工作。

吴恩达的学生中，Pieter Abbeel、Ian Goodfellow、Andrej Karpathy 属于同一脉络，都是吴恩达实验室出身。

---

### Q6: 为什么 $dz^{[2]}$ 和 $dz^{[1]}$ 的计算公式不一样？

> 时间：20260603周三
>
> 位置：35 — 3.9 神经网络的梯度下降

根源：在链式法则中所处的位置不同——$z^{[2]}$ 紧挨损失函数，$z^{[1]}$ 隔了两层才到损失。

**约定（两层网络，二分类）**

$$
\begin{aligned}
z^{[1]} &= W^{[1]} x + b^{[1]} ,\quad a^{[1]} = g^{[1]}(z^{[1]}) \\[4pt]
z^{[2]} &= W^{[2]} a^{[1]} + b^{[2]} ,\quad a^{[2]} = \sigma(z^{[2]}) \\[4pt]
\mathcal{L} &= -y\log a^{[2]} - (1-y)\log(1-a^{[2]})
\end{aligned}
$$

**$dz^{[2]}$：链式只有两段**

$z^{[2]}$ 直接经 sigmoid 进损失，无中间变量：

$$
\frac{\partial \mathcal{L}}{\partial z^{[2]}}
= \underbrace{\frac{\partial \mathcal{L}}{\partial a^{[2]}}}_{-\frac{y}{a^{[2]}}+\frac{1-y}{1-a^{[2]}}}
  \cdot \underbrace{\frac{\partial a^{[2]}}{\partial z^{[2]}}}_{a^{[2]}(1-a^{[2]})}
= a^{[2]} - y
$$

交叉熵与 sigmoid 搭配的特别之处：两项含 $a^{[2]}(1-a^{[2]})$ 的复杂表达式相乘后恰好约掉，只剩简洁的 $a^{[2]}-y$。

**$dz^{[1]}$：链式多两段**

$z^{[1]}$ 前面隔了 $a^{[1]} \to z^{[2]} \to a^{[2]}$，多出两个中间变量：

$$
\begin{aligned}
\frac{\partial \mathcal{L}}{\partial z^{[1]}}
&= \frac{\partial \mathcal{L}}{\partial z^{[2]}} \cdot
   \frac{\partial z^{[2]}}{\partial a^{[1]}} \cdot
   \frac{\partial a^{[1]}}{\partial z^{[1]}} \\[4pt]
&= dz^{[2]} \cdot W^{[2]} \cdot g^{[1]}{}'(z^{[1]})
\end{aligned}
$$

相比 $dz^{[2]}$，多了两项：
- **$W^{[2]}$**：输出层误差按权重回传
- **$g^{[1]}{}'(z^{[1]})$**：隐藏层激活函数的导数

**对比**

|          | $dz^{[2]}$（输出层） | $dz^{[1]}$（隐藏层） |
|----------|---------------------|---------------------|
| 路径     | $z^{[2]} \to a^{[2]} \to \mathcal{L}$ | $z^{[1]} \to a^{[1]} \to z^{[2]} \to a^{[2]} \to \mathcal{L}$ |
| 链式段数 | 2                    | 3                    |
| 表达式   | $a^{[2]} - y$        | $dz^{[2]} \cdot W^{[2]} \cdot g^{[1]}{}'(z^{[1]})$ |

> 注：以上是标量推导。推广到矩阵形式时，$dz^{[2]} \cdot W^{[2]}$ 需写成 $(W^{[2]})^{\top} \cdot dz^{[2]}$（转置原因见 Q7）。其中 da 作为中间桥梁的作用，详见 Q9。

---

### Q7: $(W^{[2]})^{\top}$ 和 $dz^{[2]}$ 谁在前谁在后？顺序有区别吗？

> 时间：20260603周三
>
> 位置：35 — 3.9 神经网络的梯度下降

**有区别，且顺序由维度决定，不能互换。** 矩阵乘法不满足交换律，顺序错直接维度报错。

沿用 Q6 两层网络，维度约定：
$$
a^{[1]} \in \mathbb{R}^{n^{[1]} \times 1},\qquad
W^{[2]} \in \mathbb{R}^{n^{[2]} \times n^{[1]}},\qquad
dz^{[2]} \in \mathbb{R}^{n^{[2]} \times 1}
$$

**正确公式与维度验证**

$$
dz^{[1]} = \underbrace{(W^{[2]})^{\top}}_{(n^{[1]} \times n^{[2]})} \cdot \!
           \underbrace{dz^{[2]}}_{(n^{[2]} \times 1)}
           \odot\, g^{[1]}{}'(z^{[1]})
$$

$(n^{[1]} \times n^{[2]}) \times (n^{[2]} \times 1) = (n^{[1]} \times 1)$，与 $dz^{[1]}$ 对齐。

反过来 $dz^{[2]} \, (n^{[2]} \times 1) \cdot (W^{[2]})^{\top} \, (n^{[1]} \times n^{[2]})$ —— 内维不匹配，矩阵无法相乘。

批量样本（$dz^{[2]}$ 变为 $n^{[2]} \times m$）同理：$(n^{[1]} \times n^{[2]}) \times (n^{[2]} \times m) = n^{[1]} \times m$，仍是 $W^{\top}$ 在前。

**为什么有些笔记写成 dz @ W.T？—— 行列约定不同**

|                    | 列向量（吴恩达板书）                    | 行向量（NumPy 代码）                 |
|--------------------|---------------------------------------|-------------------------------------|
| 数据排布           | $a^{[1]}$ 是 $(n^{[1]} \times 1)$     | `a1` 是 $(m,\ n^{[1]})$            |
| 前向               | $W^{[2]} a^{[1]}$                     | `a1 @ W2.T`                        |
| $dz^{[1]}$         | $(W^{[2]})^{\top} \, dz^{[2]}$        | `dz2 @ W2`                         |

- **列向量**：$W^{\top}$ 在前，$dz$ 在后
- **行向量**：`dz` 在前，`W` 在后（`W` shape 本身是 $(n^{[1]}, n^{[2]})$，无需转置）

本质是同一次矩阵乘法的两种记法，只是数据排布方向不同。**唯一不变的检验标准：乘完之后维度必须等于 $dz^{[1]}$ 的维度。**

---

### Q8: 深层网络为什么比浅层网络更高效？奇偶校验（XOR）的 O(n) vs O(2^n) 怎么理解？

> 时间：20260604周四
>
> 位置：42 — 4.4 为什么深度这么有道理

**核心结论：** 对奇偶校验（Parity Check），深层网络用 XOR 树需要 O(n) 个神经元，单隐藏层网络需要 O(2^n) 个神经元。

**什么是奇偶校验？** 判断一组二进制位中 1 的个数是奇数还是偶数。等价于对所有输入位做连续 XOR 运算（如 `1 XOR 0 XOR 1 XOR 1 = 1`）。

**深层网络怎么做的（XOR 树）？**

利用 XOR 结合律：`XOR(a,b,c,d) = XOR(XOR(a,b), XOR(c,d))`，逐层两两归约：

```
x1 x2 x3 x4 x5 x6 x7 x8    ← 输入（8位）
 \ /     \ /     \ /     \ /
  ⊕       ⊕       ⊕       ⊕   ← 第1层：n/2 个神经元，算 XOR(相邻两位)
   \     /         \     /
    ⊕               ⊕         ← 第2层：n/4 个神经元
         \         /
          ⊕                    ← 第3层：1 个神经元，输出结果
```

层数 O(log n)，总神经元数 n/2 + n/4 + ... + 1 ≈ n - 1 = **O(n)**。

**单隐藏层为什么需要 O(2^n)？**

```
隐藏层（8个神经元）                  输出层
                      
 [ 神经元1：只认 000 ]    (没连线)
 [ 神经元2：只认 001 ] ───(有线连接)───┐
 [ 神经元3：只认 010 ] ───(有线连接)───┼─► [ 输出层 ] ──► 亮！(输出1)
 [ 神经元4：只认 011 ]                │  (只要连着的线里有任何一个传来了1，它就亮)
       ...                          │
 [ 神经元8：清认 111 ] ───(有线连接)───┘
```

**直觉对比：** 深层网络像数学推导，逐步化简；单隐藏层像查字典，把 2^n 种情况和答案全部背下来。

---

### Q9: 输出层 $dz^{[2]} = a^{[2]} - y$ 这么简单，为什么还需要 $da$？

> 时间：20260604周四
>
> 位置：35-36 — 3.9 神经网络的梯度下降 / 3.10 反向传播的直觉

**输出层 $dz^{[2]} = a^{[2]} - y$ 看起来不需要 $da$，但其实 $da$ 被"藏"在了约分里。** 隐藏层没有这种约分巧合，必须显式使用 $da$。

**输出层：$da$ 被约掉了**

链式法则的标准步骤中，$da^{[2]}$ 是必经之路：

$$
dz^{[2]} = \underbrace{\frac{\partial L}{\partial a^{[2]}}}_{da^{[2]}} \cdot \underbrace{\frac{\partial a^{[2]}}{\partial z^{[2]}}}_{\sigma'(z^{[2]})}
$$

逐项计算：

$$
da^{[2]} = \frac{\partial}{\partial a^{[2]}} \Big[ -y\log a^{[2]} - (1-y)\log(1-a^{[2]}) \Big]
= \frac{a^{[2]} - y}{a^{[2]}(1-a^{[2]})}
$$

$$
\sigma'(z^{[2]}) = a^{[2]}(1-a^{[2]})
$$

相乘：

$$
dz^{[2]} = \frac{a^{[2]} - y}{\cancel{a^{[2]}(1-a^{[2]})}} \cdot \cancel{a^{[2]}(1-a^{[2]})} = a^{[2]} - y
$$

**这是交叉熵 + sigmoid 搭配的数学巧合**——分母恰好与激活函数的导数消掉。$da^{[2]}$ 真实存在，只是被约分隐藏了。

**隐藏层：$da$ 必须显式存在**

$dz^{[1]}$ 的链式路径中，$a^{[1]}$ 与损失 $L$ 之间隔了 $z^{[2]}$ 和 $a^{[2]}$：

$$
dz^{[1]} = \frac{\partial L}{\partial a^{[1]}} \cdot \frac{\partial a^{[1]}}{\partial z^{[1]}}
$$

其中 $\frac{\partial L}{\partial a^{[1]}}$（即 $da^{[1]}$）无法被约掉，因为它必须要从后面的层传回来：

$$
da^{[1]} = (W^{[2]})^{\top} \, dz^{[2]}
$$

最终：

$$
dz^{[1]} = \underbrace{(W^{[2]})^{\top} \, dz^{[2]}}_{da^{[1]}} \; \odot \; g^{[1]}{}'(z^{[1]})
$$

**总结**

|          | 输出层 $dz^{[2]}$ | 隐藏层 $dz^{[1]}$ |
|----------|-------------------|-------------------|
| $da$ 是否显式出现 | 被约掉，不显式出现 | 必须显式计算 |
| 为什么 | 交叉熵 + sigmoid 分母巧合 | 没有真实标签对比，依赖后层回传的 $da$ 得知误差 |
| $da$ 的真实作用 | 仍然被计算了，只是形式上简化了 | 梯度传回前一层的唯一桥梁 |

$da$ 在数学上始终存在，输出层中只是"简化了的特例"而非"不需要"。

---

### Q10: 为什么梯度检验需要关闭 Dropout？不能固定随机掩码来检验吗？

> 时间：20260606周六
>
> 位置：60 — 1.14 梯度检查实施须知

**为什么必须关闭 Dropout：**

梯度检验的核心是比较 $J(\theta+\epsilon)$ 和 $J(\theta-\epsilon)$ 之差与解析梯度的吻合度。前提是 $J$ 必须是 $\theta$ 的确定函数。Dropout 每次前向传播随机丢弃不同神经元，导致：
- 算 $J(\theta+\epsilon)$ 时丢弃的是 A 组神经元，算 $J(\theta-\epsilon)$ 时丢弃的是 B 组神经元。
- 两次 $J$ 的差异既包含参数扰动 $\epsilon$ 的影响，也包含网络结构随机变化的影响，算出来的数值梯度是噪音，无法与解析梯度对齐。

**为什么不能固定随机掩码来做检验？**

从数学上讲可行，但工程上不这样做，原因有三：

1. **违背测试初衷**：梯度检验的唯一目的是验证反向传播代码是否正确。关 Dropout 只需改一个参数（`keep_prob=1.0`），固定掩码则需要修改前向/反向传播核心逻辑，把掩码作为变量提取、保存、复用，徒增复杂度，容易在测试代码本身引入新 Bug。

2. **测试覆盖率变差**：固定掩码后检验的只是一个残缺的子网络，而非完整的全连接网络。关闭 Dropout 能一次性对所有参数和所有连接路径做全覆盖检验，更严谨。

3. **框架限制**：现代框架（PyTorch/TensorFlow）的 Dropout 层随机数生成发生在底层 C++/CUDA，在 Python 层精准记录并复现某次随机掩码操作困难且低效。

**正确流程**：关 Dropout（`keep_prob=1.0`）→ 跑梯度检验确认代码无误 → 恢复原 keep_prob 开始训练。

---

### Q11: 指数加权平均中，前 t 项的权重之和为什么等于 $1-\beta^t$？

> 时间：20260607周日
>
> 位置：65 — 2.5 指数加权平均数的偏差修正

**问题背景：** 指数加权平均公式为 $v_t = \beta v_{t-1} + (1-\beta)\theta_t$，展开后每一项都有权重系数。初始 $v_0=0$，导致初始阶段预测值偏低。修正方法是除以 $1-\beta^t$。这个分母的来源是前 $t$ 项权重之和。

**展开公式包含完整项：**

$$
v_t = (1-\beta)\theta_t + (1-\beta)\beta\theta_{t-1} + \cdots + (1-\beta)\beta^{t-1}\theta_1 + \underbrace{\beta^t v_0}_{=0}
$$

**推导：** 前 $t$ 项的权重和 $S$ 为：

$$
S = (1-\beta) + (1-\beta)\beta + (1-\beta)\beta^2 + \cdots + (1-\beta)\beta^{t-1}
$$

提取公因子 $(1-\beta)$：

$$
S = (1-\beta)(1 + \beta + \beta^2 + \cdots + \beta^{t-1})
$$

括号内是等比数列，用等比数列求和公式：

$$
1 + \beta + \cdots + \beta^{t-1} = \frac{1-\beta^t}{1-\beta}
$$

代入：

$$
S = (1-\beta) \cdot \frac{1-\beta^t}{1-\beta} = 1-\beta^t
$$

**结论：** $v_t$ 前面各项权重之和只有 $1-\beta^t$ 而不是 1，这就是初始预测偏低的数学来源。除以 $1-\beta^t$ 将权重和归一化为 1，修正偏差。随着 $t$ 增大，$\beta^t \to 0$，$1-\beta^t \to 1$，修正效果自然消失。

---

### Q12: 如何理解 RMSprop 优化算法？

> 时间：20260607周日
>
> 位置：67 — 2.7 RMSprop

可以把 RMSprop 理解为**"智能减震器"**，专门解决标准梯度下降在狭长山谷中的来回震荡问题。

**1. 痛点：为什么会有震荡？**

假设代价函数等高线呈狭长椭圆形，横向 $W$ 平缓，纵向 $b$ 陡峭。标准梯度下降时：
- $b$ 方向梯度大，上下剧烈震荡。
- $W$ 方向梯度小，前进极其缓慢。
- 调大学习率 $\alpha$ 会让 $b$ 方向的震荡直接失控甚至发散。

**2. 核心思想**

在陡峭的方向（$b$）踩刹车，在平缓的方向（$W$）踩油门。

**3. 公式**

第 $t$ 次迭代，给定 mini-batch 梯度 $dW$ 和 $db$：

第一步，计算梯度平方的指数加权移动平均：

$$
S_{dW} = \beta S_{dW} + (1-\beta) \, dW^2
$$

$$
S_{db} = \beta S_{db} + (1-\beta) \, db^2
$$

（平方是逐元素运算。$\beta$ 通常取 $0.999$。）

第二步，参数更新（将累积值作为分母）：

$$
W = W - \alpha \frac{dW}{\sqrt{S_{dW}} + \epsilon}
$$

$$
b = b - \alpha \frac{db}{\sqrt{S_{db}} + \epsilon}
$$

（$\epsilon = 10^{-8}$，防止除零。）

**4. 为什么能消除震荡？**

结合狭长山谷例子：

- **陡峭方向 $b$**：每次 $db$ 很大 $\rightarrow$ $db^2$ 很大 $\rightarrow$ $S_{db}$ 累积为大数 $\rightarrow$ 更新时除以大数 $\rightarrow$ 步伐被压缩，消除震荡。
- **平缓方向 $W$**：每次 $dW$ 很小 $\rightarrow$ $dW^2$ 很小 $\rightarrow$ $S_{dW}$ 累积为小数 $\rightarrow$ 更新时除以小数 $\rightarrow$ 步伐被放大，加速前进。

最终：纵向无用震荡被抹平，横向有效推进被保留。可以用更大的学习率而不会左摇右晃。

---

### Q13: 受限玻尔兹曼机（RBM）是什么？

> 时间：20260609周二
>
> 位置：105 — 2.11 Ruslan Salakhutdinov访谈

RBM 是一个两层的神经网络结构：一层接收输入数据（可见层），一层提取特征（隐藏层）。两层之间全连接，同一层内部没有连接——这就是"受限"的意思，和无限制玻尔兹曼机（同层也可连接）区分开。

**核心思想**

RBM 定义了一个能量函数，数据越"真实"能量越低，越"假"能量越高。训练的目标就是让真实数据对应低能量状态、幻觉数据对应高能量状态。

**怎么训练的？——一次正向-重构循环**

1. 把数据喂入可见层
2. 算隐藏层该怎么激活
3. 反过来从隐藏层重建一份数据
4. 比较"原始数据"和"重建数据"的差异，据此更新权重

**直觉**：第 1 步是"看一遍输入"，第 3 步是"凭印象画出来"。画画的好坏就反映了模型对数据理解的程度，差异用来修正模型。一次循环就够了，不需要反复迭代。

**为什么曾是核心方法？**

2005-2009 年，直接用反向传播训练深层网络做不动——没有 ReLU 所以梯度会消失，CPU 又太慢，训练要几个月。Hinton 和 Salakhutdinov 想到的办法是：先用 RBM 逐层做无监督预训练，让每层各自"热身"找到合理的起点，全搭好后再用反向传播整体微调。这个技巧首次让极深的网络能够成功训练，是深度学习复兴的关键一步。

**为什么后来退场了？**

GPU 兴起 + ReLU + BatchNorm + 好的初始化方法出现后，直接反向传播就能训深层网络，效果相当甚至更好。RBM 的训练依赖采样、速度慢、也不好适配卷积等现代架构，就逐渐退出了。

**今天还有什么影响？**

RBM 作为具体方法已被淘汰，但它"无监督预训练 + 微调"的核心思路没有消失。BERT、GPT 本质也是先在海量无标签文本上预训练，再针对具体任务微调——只是把 RBM 换成了 Transformer，把逐层预训练换成了端到端语言模型预训练。

---

### Q14: 讲解 GloVe 模型公式

> 时间：20260614周日
>
> 位置：175 — 2.8 GloVe 词向量

**Q: 给我讲解一下吴恩达讲解的这个 GloVe 模型公式：**

$$
\min \sum_i \sum_j f(X_{ij})(\theta_i^T e_j + b_i + b_j' - \log X_{ij})^2
$$

这个公式是 GloVe（Global Vectors for Word Representation）模型的核心损失函数（Loss Function）。它是斯坦福大学提出的一种结合了全局词频统计（Matrix Factorization）和局部上下文窗口（Skip-gram）优点的词向量训练模型。

吴恩达在课程中指出，这个公式看起来很复杂，但它的核心思想极其纯粹：让任意两个词的向量内积（加上偏置），去拟合这两个词共同出现的次数（的对数）。

为了彻底理解它，我们可以把这个公式拆解成三个部分：核心预测目标、平方误差 和 权重函数。

**1. 核心预测目标：他在拟合什么？**

先看括号里面的核心部分：

$$
\theta_i^T e_j + b_i + b_j' \approx \log X_{ij}
$$

- $X_{ij}$：表示词 $i$ 和词 $j$ 在整个语料库中共同出现在同一个上下文窗口中的次数（共现次数）。
- $\log X_{ij}$：对共现次数取对数。为什么要取对数？因为自然语言中词频分布极不均匀（符合齐普夫定律），像 "the"、"and" 和某些高频词的共现次数可能几十万次，而稀有词只有几次。取对数可以压缩这个范围，防止高频词完全主导训练。
- $\theta_i$ 和 $e_j$：分别是词 $i$ 作为"上下文词（Context）"和词 $j$ 作为"中心词（Target）"时的词向量。
- $\theta_i^T e_j$：这两个向量的内积（点积）。在线性代数中，内积越大，意味着两个向量在空间中的方向越接近，即语义越相似、共现概率越高。
- $b_i$ 和 $b_j'$：分别是词 $i$ 和词 $j$ 的偏置项（Bias）。这两个偏置用来吸收单词自身的"特殊性"（比如某些词本身就是天生的高频词，不管跟谁在一起出现次数都多）。

一句话总结这部分：我们希望通过调整词向量 $\theta_i$ 和 $e_j$，使得它们的相似度 $\theta_i^T e_j$（加上各自的偏置）尽可能接近它们在现实语料库中的共现对数 $\log X_{ij}$。

**2. 平方误差：目标是误差最小化**

有了预测值和真实值，接下来就是计算它们之间的差距：

$$
(\theta_i^T e_j + b_i + b_j' - \log X_{ij})^2
$$

这就是最经典的均方误差（MSE）结构。如果模型预测的相似度与现实中的 $\log X_{ij}$ 差得很大，平方后的惩罚就会很大。

最外层的 $\sum_i \sum_j$ 表示我们要遍历语料库中所有可能的单词对 $(i,j)$，把所有词对的误差都加起来。整个公式的目标是 $\min$（最小化），即通过梯度下降让总误差达到最小。

**3. 权重函数 $f(X_{ij})$：解决致命缺陷**

如果公式只有后面那部分，会有一个严重的数学和逻辑漏洞：

- **当 $X_{ij}=0$ 时怎么办？** 如果两个词在语料库里从未共同出现过，$\log(0)$ 在数学上是负无穷（$-\infty$），公式直接崩溃。
- **高频词噪音太大**：像 "is", "the" 这样的词，跟任何词的共现次数 $X_{ij}$ 都极大，如果不加控制，模型会为了迎合这些无意义的高频词对，而牺牲掉真正有语义的稀有词对。

为了解决这个问题，原作者引入了权重函数 $f(X_{ij})$。它的数学定义通常是：

$$
f(x) =
\begin{cases}
\left(\frac{x}{x_{max}}\right)^{\alpha} & \text{if } x < x_{max} \\[4pt]
1 & \text{if } x \geq x_{max}
\end{cases}
$$

（通常设 $x_{max}=100$, $\alpha=0.75$）

这个函数起到了三个神级效果：

- **$f(0)=0$**：如果两个词没共现过（$X_{ij}=0$），权重直接为 0，这一项误差直接归零，完美绕过了 $\log(0)$ 的数学灾难。
- **不宠溺高频词**：当共现次数超过一定阈值（如 $x_{max}=100$）后，$f(X_{ij})$ 封顶死死地等于 1。这就意味着，共现 10 万次的两个词，其权重和共现 100 次的词是一样的，防止了高频词霸榜。
- **鼓励中频词**：对于共现次数较少（小于 100）的词对，给予一个相对有分量的权重，让稀有词也能得到充分训练。

**总结吴恩达的观点：**

- **矩阵分解的影子**：GloVe 的本质是在对一个巨大的"词共现矩阵"进行低维近似分解。
- **对称性**：在公式中，$\theta_i$ 和 $e_j$ 的地位是完全对称的。在实际训练结束后，GloVe 通常会将最终得到的 $\theta_i$ 和 $e_i$ 相加（即 $\theta_i + e_i$）作为该词的最终词向量，这样能让结果更加稳定。

### Q15: $(\theta_i^T e_j)$ 和 $\log X_{ij}$ 尺度不同，不需要归一化吗？

> 时间：20260614周日
>
> 位置：175 — 2.8 GloVe 词向量

这是一个非常敏锐的数学直觉。在绝大多数机器学习任务中，如果两个特征或数值的尺度相差巨大，直接相减确实会导致大尺度数据完全主导梯度，导致网络无法收敛。

但在这个特定的 GloVe 公式中，它们在本质上已经被"归一化"到同一个尺度和维度上了，因此不需要再做额外的归一化。

**1. 概念误区：$\theta_i^T e_j$ 并不是普通的向量乘积**

你可能会直觉地认为，词向量 $\theta_i$ 和 $e_j$ 内部的数值是可以无限增长的，而 $\log X_{ij}$ 的范围很小。但实际上，词向量的初始化和它们要拟合的目标决定了它们的尺度。

- $\log X_{ij}$ 是真正的"因变量"（Label/目标值）：在这个监督学习公式中，$\log X_{ij}$ 是雷打不动的"真理"。
- $\theta_i^T e_j$ 是"自变量"（预测值）：模型在训练开始时，词向量 $\theta_i$ 和 $e_j$ 是用非常小的随机数（比如在 $[-0.5,0.5]$ 之间）初始化的。此时它们的内积 $\theta_i^T e_j$ 也很小。

随着梯度下降的进行，词向量中的参数是在主动向 $\log X_{ij}$ 的尺度靠拢。换句话说，$\theta_i^T e_j$ 本身就是为了去模仿（拟合）对数词频的量纲。它们在数学逻辑上是同一种东西。

**2. 数值尺度的真实范围：它们其实惊人地一致**

代入真实语料库的极端数据来看一下它们两者的数值范围：

- $\log X_{ij}$ 的最大可能值：假设在极大的语料库（如包含数十亿词的 Wikipedia）中，两个最频繁在一起的词（比如 "of the"）共同出现了 $X_{ij}=10,000,000$（一千万）次。$\log(10,000,000) \approx 16.1$。也就是说，整个语料库里对数目标的最大值通常不会超过 16 到 20。
- $\log X_{ij}$ 的最小可能值：如果两个词只共同出现过 1 次，那么 $\log(1)=0$。（如果出现 0 次，权重函数 $f(0)=0$ 直接把这一项抹去了。）

因此，$\log X_{ij}$ 的真实数值区间非常稳定，就在 $[0,17]$ 之间。

再看 $\theta_i^T e_j$：如果词向量的维度是 $d=300$。两个向量的内积如果要达到 16，相当于这 300 个维度里的每一个维度的数值平均只需要达到：

$$
\sqrt{\frac{16}{300}} \approx 0.23
$$

也就是说，词向量里的每一个具体数值只需要在 $[-1,1]$ 这种极小的、健康的范围内波动，其点积就能轻松覆盖 $[0,17]$ 的区间。

**3. 为什么做额外的归一化（如余弦相似度）反而会坏事？**

你可能会想，如果把 $\theta_i^T e_j$ 强制限制成余弦相似度（Cosine Similarity，范围 $[-1,1]$），不是更标准吗？

如果那样做，模型就彻底崩了。因为余弦相似度抹去了向量的长度（Magnitude）信息，它的最大值只能是 1。如果预测值的上限是 1，而目标 $\log X_{ij}$ 却能达到 16，模型无论怎么努力，平方误差都永远无法缩小。

**总结**

GloVe 聪明的点就在于，它让词向量的长度本身承载了信息。词向量的内积 $\theta_i^T e_j$ 既包含了"方向（语义相似度）"，也包含了"长度（词频总量）"。既然它的目标就是去逼近 $[0,17]$ 之间的对数词频，那么在梯度下降的驱使下，词向量自然会自己调节尺度去和 $\log X_{ij}$ 完美对齐，不需要人工强加干预。

---

### Q16: 注意力机制的上下文公式为什么要用求和（∑）而不是拼接？

> 时间：20260615周一
>
> 位置：185 — 3.8 注意力模型

上下文公式：
$$
c^{\langle t \rangle} = \sum_{t'=1}^{T_x} \alpha^{\langle t, t' \rangle} a^{\langle t' \rangle}
$$

（$a^{\langle t' \rangle}$ 是编码器第 $t'$ 个位置的激活，$\alpha^{\langle t, t' \rangle}$ 是 softmax 归一化后的注意力权重，$\sum\alpha=1$）

**一、为什么要求和？（三点原因）**

1. **物理意义：加权平均。** $c^{\langle t \rangle}$ 是对所有输入位置激活的加权平均。类比期末总成绩 = 作业×20% + 期中×30% + 期末×50%——不求和只能得到零散的单项分数，得不到"总成绩"。

2. **动态聚焦。** 翻译强对应词（如 "cat"）时，对应位置 $\alpha\approx1$，其他 $\approx0$，$c$ 几乎等于对应 $a$。需要综合语境（如倒装句）时，权重分散到多个位置，求和将它们打包为一个向量。不求和就只能每次死板看一个词。

3. **消除不定长输入的影响。** 每个 $a^{\langle t' \rangle}$ 维度固定（如 256），但句子长度 $T_x$ 可变。求和将 $T_x$ 个向量压缩为一个固定维度（256），Decoder 全连接层参数矩阵维度必须固定。

**二、为什么不能直接拼接？**

A. **无法处理不定长输入。** 5 词句子拼接后 1280 维，20 词 5120 维。Decoder 全连接层参数矩阵维度固定，无法接收忽大忽小的输入。

B. **拼接抹杀了"注意力"的筛选意义。** 拼接意味着所有输入全额排成一排塞给 Decoder，退化回让 Decoder 自己大海捞针，失去了注意力主动挑选、动态减负的能力。

**三、为什么求和不会变成"四不像"的混合信息？**

1. **高维空间的正交性。** 在 256/512 维空间里，随机两个向量几乎正交。加权求和是互不干扰的信号叠加，Decoder 的线性变换（矩阵乘法）有很强的解耦能力，能从叠加信号中抽取目标分量。

2. **注意力权重是稀疏的。** Softmax 后的 $\alpha$ 极度两极分化：相关词 $\alpha$ 可达 0.9，无关词只有 0.001~0.01。求和结果由高权重词绝对主导，低权重词只是微调噪音/语境补充，不是真正意义上的"搅拌融合"。

**补充：** Multi-Head Attention 中，每个 Head 内部用求和压缩时序，8 个 Head 的结果再拼接——先求和解决不定长，再拼接丰富特征。