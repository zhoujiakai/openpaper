每天学完之后需要在对应主题下的日志里面进行回顾总结。即使只有简单的几句话。





**卡帕西在15年发布的一篇构想未来 AI 场景的文章：http://karpathy.github.io/2015/11/14/ai/**

**卡帕西的另一个项目：https://github.com/karpathy/nanochat**

- nanochat 是 Andrej Karpathy 打造的一个**极简但完整的 LLM 训练实验平台**，只需调一个 --depth 参数就能在单节点 8XH100 上约 2 小时内完整训练出一个 GPT-2 级别（~$48 成本）的模型，并直接在 Web UI 里像  ChatGPT 一样和它聊天。

> 什么时候看下。



**卡帕西的单文件项目：https://gist.github.com/karpathy/8627fe009c40f57531cb18360106ce95**

这是 **Andrej Karpathy** 写的 microgpt.py —— 一个**纯 Python 实现的微型 GPT（Generative Pretrained**     

 **Transformer）**，不依赖任何第三方库。                                                                              

 核心特点：                                                                                         

1. **从零实现完整的** **GPT** **算法**，包含：                                  

  \- **Value** **类**：一个微型自动求导引擎（Autograd），实现反向传播

  \- **参数初始化**：词嵌入矩阵、位置嵌入、注意力权重（Q/K/V/O）、MLP 权重

  \- **Transformer** **架构**：Multi-head Self-Attention + MLP block，带残差连接和 RMSNorm 归一化

  \- **训练循环**：Cross-entropy loss + Adam 优化器，线性学习率衰减

  \- **推理/采样**：自回归生成 + temperature 控制

2. **数据集**是一个名字列表（names.txt），所以训练和生成的是**类人名的字符串**。
3. **代码极简**：约 200 行，没有任何依赖（除了标准库），是理解 GPT 工作原理的教学级实现。模型很小：1 层

 Transformer，16 维嵌入，4 个注意力头，总参数量约几千个。

 简单说，这就是 Karpathy 用来展示 "GPT 最本质长什么样" 的教学代码。



先刷分吧，把这个月的分数刷到90分再说。

感觉每一小节都像一小块压缩饼干，消化几块就饱了。



有很多重复的内容，可以重新看一次，就当作复习，也可以只看第四门课和第五门课。





# 循环

```python
看过视频 = False
while True:
  if "00:00" <= 当前时间 <= "08:00":
    休息;
  elif 看过视频:
    try:
    	写笔记;
    except Exception as e:
      喝水;
    finally:
    	看过视频 = False;
  else:
    看视频;
    看过视频 = True;
```





b站视频：https://www.bilibili.com/video/BV12urfB1E7s



才发现吴恩达的课程分机器学习和深度学习，深度学习28.5小时，比机器学习还多9小时。



这个课程是2018年的，画质有点糊，之后吴恩达没有再翻版重录。







# <p align="center">日志

**20260611周四**

- ml-吴恩达深度学习，学到78.7%，(78.7-66)x0.01x28.5x3=10.859 小时

从效率网络，到数据增强，到提高比赛 Benchmark 的得分的方法。

从目标分类定位到 YOLO 到 R-CNN系列，到人脸识别到 U-Net，到风格迁移的整体框架，最后实现内容代价函数和风格代价函数。

**20260610周三**

- ml-吴恩达深度学习，学到66%，(66-55.3)x0.01x28.5x3=9.149 小时

从各种边缘检测的卷积核学到 MobileNet V2 在移动设备上进行低成本计算。学习了卷积中的填充和步长。

学习了残差连接。学习了各种不同卷积层前后嵌套的 Inception 网络，被称为网络中的网络。学习了用于降低移动端计算成本的 MobileNet V1 的深度可分离卷积 和 V2 的 Bottleneck 模块。





























