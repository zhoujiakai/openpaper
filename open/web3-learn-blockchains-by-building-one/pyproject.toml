[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "build-blockchains-by-building-one"
version = "0.1.0"
description = "A blockchain project"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.3",
    "flask>=3.0.3",
]
